<div class="max-w-3xl mx-auto bg-white shadow-md rounded-xl p-6 mt-8 border">
  <h2 class="text-2xl font-bold text-indigo-700 mb-4">{{product.title}}</h2>
  <p class="text-gray-600 mb-3">{{product.description}}</p>

  <div class="mb-4">
    <p class="text-lg text-gray-800 font-semibold">
      💲 Precio: ${{product.price}}
    </p>
    <p class="text-sm text-gray-600">🏷️ Categoría: {{product.category}}</p>
    <p class="text-sm text-gray-600">📦 Stock disponible: {{product.stock}}</p>
  </div>

  <button
    onclick="addToCart('{{product._id}}')"
    class="w-full bg-indigo-600 text-white py-2 rounded hover:bg-indigo-700"
  >
    🛒 Agregar al carrito
  </button>

  <div class="mt-6 text-center">
    <a
      href="/products"
      class="text-indigo-600 hover:underline text-sm"
    >
      ⬅️ Volver al catálogo
    </a>
  </div>
</div>

<script>
  async function addToCart(productId) {
    try {
      let cartId = localStorage.getItem('cartId');

      if (!cartId) {
        const createResponse = await fetch('/api/carts', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
        });

        if (!createResponse.ok) {
          alert('❌ No se pudo crear el carrito.');
          return;
        }

        const createData = await createResponse.json();
        cartId = createData?.payload?._id;
        if (!cartId) {
          alert('❌ El servidor no devolvió un ID de carrito válido.');
          return;
        }
        localStorage.setItem('cartId', cartId);
      }

      const addResponse = await fetch(`/api/carts/${cartId}/products/${productId}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ quantity: 1 }),
      });

      if (addResponse.ok) {
        alert('✅ Producto agregado al carrito correctamente.');
      } else {
        alert('❌ El servidor no pudo agregar el producto al carrito.');
      }
    } catch (error) {
      console.error('❌ Error al agregar al carrito:', error);
      alert('❌ Ocurrió un error al agregar el producto al carrito.');
    }
  }
</script>
